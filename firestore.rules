rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Simple open rules for testing - ALLOW EVERYTHING
    match /{document=**} {
      allow read, write: if true;
    }
  }
}